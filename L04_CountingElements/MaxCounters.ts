function solution(N: number, A: number[]): number[] {
  let length: number = A.length;
  let counters: number[] = new Array(N).fill(0);

  let indexer: number[] = new Array(length).fill(0);
  indexer[0] = N;

  let maxApplied: boolean = false;
  let max: number = 0;

  return counters;
}
